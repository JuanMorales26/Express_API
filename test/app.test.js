const app = require('../app/app')
const request = require("supertest");

/**describe("Unit test for Express_API_HTTP_Methods", () => {
    test("GET /v1/explorers", (done) => {
      request(app)
        .get("/v1/explorers")
        .expect(200)
        .expect((res) => {
            expect(res).toBe('Culo')
        })
        .end((err, res) => {
          if (err) return done(err);
          return done();
        })
    })
})*/